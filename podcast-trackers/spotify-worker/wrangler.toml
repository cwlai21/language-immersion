name = "ecoute-spotify"
main = "worker.js"
compatibility_date = "2026-07-01"
workers_dev = true

# Timer state moved to Supabase (kv_state table) — Workers KV's eventual
# consistency broke read-after-write for the overlap guard.

[triggers]
crons = ["*/2 * * * *"]

[observability]
enabled = true
